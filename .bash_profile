# Trigger ~/.bashrc commands
. /Users/tomshea/.bashrc

# Source each dotfile
    # for DOTFILE in `find /Users/tomshea/Documents/Code/.dotfiles`
    # do
    #   [ -f “$DOTFILE” ] && source “$DOTFILE”
    # done
